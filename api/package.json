{
  "name": "ps-quote-api",
  "version": "1.0.0",
  "private": true,
  "description": "Backend API for the PS Quote Builder tool - stores saved quotes in SQLite so they can be picked up from another device.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "express": "^4.19.2"
  }
}
